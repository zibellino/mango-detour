<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    android:name is a fully qualified reference to androidx.activity's own
    ComponentActivity class — already a dependency here, already concrete
    and instantiable, default onCreate() does nothing beyond lifecycle
    setup. No custom Kotlin file needed for a blank launcher screen.

    Replace with your own Activity (".MainActivity", package-qualified in
    the usual way) once you're writing real UI.
    -->
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
